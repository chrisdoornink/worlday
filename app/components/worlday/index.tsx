'use client';

import React from 'react';
import styles from './styles.module.css';

const World = () => {
  return (
    <div className={styles.worldContainer}>
      {/* We can add more world elements here later */}
    </div>
  );
};

export default World;
